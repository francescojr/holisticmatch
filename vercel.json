{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://44.197.112.222/api/:path*"
    }
  ]
}
